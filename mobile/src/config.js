// Everkin mobile app configuration.
// IMPORTANT: set this to your deployed backend URL (the same URL as the web app).
// Example: https://your-app.preview.emergentagent.com  (do NOT add a trailing slash)
export const BACKEND_URL = "https://ai-form-filler-7.preview.emergentagent.com";
export const API = `${BACKEND_URL}/api`;
